import React from 'react';
import Thread from '../components/Thread';

const Home = () => {
    return (
        <div className='home'>
            <Thread />
        </div>
    );
};

export default Home;